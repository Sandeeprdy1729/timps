{
  "name": "@timps-plugin/shell",
  "version": "0.1.0",
  "description": "TIMPS plugin: shell utilities (run commands, pipe, background jobs) for AI agents — execute terminal operations from LLM workflows.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@timps-ai/plugin-sdk": "*"
  },
  "keywords": ["timps-plugin", "shell"],
  "license": "MIT"
}
