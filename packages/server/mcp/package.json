{
  "name": "timps-mcp-server",
  "version": "1.0.0",
  "description": "TIMPS MCP Server — Model Context Protocol server for Claude Desktop, Cursor, Windsurf, and MCP-compatible AI tools. Persistent memory and intelligence tools.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
