{
  "name": "@timps-plugin/git",
  "version": "0.1.0",
  "description": "TIMPS plugin: enhanced git tools (commit, push, PR, stash, review) for AI coding agents and automated workflows.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@timps/plugin-sdk": "*"
  },
  "keywords": ["timps-plugin", "git"],
  "license": "MIT"
}
