{
  "name": "@timps/connection-manager",
  "version": "1.0.0",
  "description": "Connection manager for TIMPS integrations — manage external service connectivity, retries, and health checks for AI agent workflows.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.2.6"
  }
}
