{
  "name": "@timps/memory-core-rs",
  "version": "0.1.0",
  "description": "TIMPS memory-core — Rust native addon (NAPI-RS). 10× faster storage, search, and hashing.",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "memory-core-rs",
    "targets": [
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "x86_64-unknown-linux-gnu",
      "aarch64-unknown-linux-gnu",
      "x86_64-unknown-linux-musl",
      "aarch64-unknown-linux-musl",
      "x86_64-pc-windows-msvc",
      "aarch64-pc-windows-msvc"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release --no-dts",
    "build:debug": "napi build --platform --no-dts",
    "test": "cargo test",
    "typecheck": "echo 'no TS typecheck for Rust crate'",
    "clean": "cargo clean && rm -f *.node"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "*.node"
  ],
  "keywords": ["napi", "rust", "memory", "timps"],
  "author": "TIMPS Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sandeeprdy1729/timps"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  }
}
